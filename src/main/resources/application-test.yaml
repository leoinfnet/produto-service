spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/produto_test
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
server:
  port: 8082